1. jekyll serve --watch

2. Downgrade directory_watcher, to fix auto reload
   sudo gem uninstall directory_watcher && sudo gem install directory_watcher -v 1.4.1

   doesn't do anything, I should just again upgrage.
   note that directory_watcher dosn't work with config files

3. markdown cheat sheet
   https://github.com/adam-p/markdown-here/wiki/Markdown-Cheatsheet
   http://blog.teamtreehouse.com/explaining-markdown

4. A samlple jekyll blog starting point
   http://www.smashingmagazine.com/2014/08/01/build-blog-jekyll-github-pages/
   https://github.com/barryclark/jekyll-now

   - git clone <the above starting point repo>
   - change _config.yml file, like changing blog name etc.
   - push changes
   - access your site at yourgitusername.github.io
   - your changes will be reflected after sometime on github

   when I tried to push I got this message authentication problem.
   Run the following commands -- (DO NOT use --global argument which will change your git profile for you system,
   without global it will just set profile only for the current repo)
   $ git config  user.email "you@example.com"
   $ git config  user.name "Your Name"

5. How to install a gem, a sample
   $ gem install jemoji jekyll-sitemap


6. working with drafts...
   http://jekyllrb.com/docs/drafts/

7. markdown supports html perfectly. note markdown treats html as it's superset.

8. post excert - didn't work comepletely, for now just hardcoded <read more> link index.html

9. Need to check updated domian configuration to make use of CDN
https://github.com/blog/1715-faster-more-awesome-github-pages
